name: CI

on:
  pull_request:
    branches:
      - develop
      - staging
      - main
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: OGS-portal-app/package-lock.json

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: OGS-portal-app/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('OGS-portal-app/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        working-directory: OGS-portal-app
        run: npm ci

      - name: Run ESLint
        working-directory: OGS-portal-app
        run: npm run lint

      - name: TypeScript type check
        working-directory: OGS-portal-app
        run: npm run typecheck || true

      - name: Run Vitest unit tests
        working-directory: OGS-portal-app
        run: npm run test

      - name: Build Vite app
        working-directory: OGS-portal-app
        run: npm run build

    env:
      NODE_ENV: production
      # Add environment variables here, using GitHub secrets
      # Example: FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
